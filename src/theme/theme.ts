import { createTheme } from '@mui/material';

const theme = createTheme({
    palette: {
        primary: {
            main: '#31572C',
        },
        secondary: {
            main: '#EDFFEC',
            light: '#e7c899',
            dark: '#9d8259',
        },
    }
});

export default theme;